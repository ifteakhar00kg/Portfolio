gemini:
  api:
    key: ${GEMINI_API_KEY}

server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/portfolio_db
    username: ifteakarahmed
    password: ""
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: ifteakarahmed.kg@gmail.com
    password: uujkdkdhfgeemxqa
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true